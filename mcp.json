{
	"mcpServers": {
		"instance": {
			"url": "http://127.0.0.1:8000/mcp"
		},
		"cloud": {
			"url": "${SURREALDB_MCP_URL}",
			"headers": {
				"Authorization": "Bearer ${SURREALDB_TOKEN}"
			}
		}
	}
}
